postgres {
  include "postgres-reference.conf"
  url = "jdbc:postgresql://localhost:5432/postgres"
  user = "postgres"
  password = "postgres"
}

jmx {
  include "jmx-reference.conf"
}

portChecks {
  include "port-checks-reference.conf"
}
